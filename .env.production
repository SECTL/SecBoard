# Default production build targets a same-origin backend.
# Use `bun run build:frontend` / `pnpm run build:frontend` for pure frontend mode.
VITE_PURE_FRONTEND=false
# VITE_LANSTART_API_BASE=https://api.example.com
# VITE_LANSTART_API_TOKEN=your-token-here
